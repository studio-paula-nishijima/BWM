#include "src/stage1_app.h"

// Arduino IDE entry point. The implementation remains in src/ so it is shared
// with the PlatformIO build.
void setup() { stage1Setup(); }
void loop() { stage1Loop(); }
